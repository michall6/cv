export default (state, action) => {
    const { type, payload } = action;
    switch (type) {
        case "CREATE_RUSUME":

        break;
        case "MANAGEMENT_RUSUME":

        break;
        default:
            break;


    }
    return state;
}
 
